{
  "name": "systemone-pi-mvp",
  "version": "0.3.0",
  "private": true,
  "description": "Local-first SystemONE Pi MVP with hardware-safe simulation and diagnostics",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check lib/hue.js && node --check lib/storage.js && node --check lib/diagnostics.js && node --check web/app.js && node --check scripts/selftest.js",
    "test": "node scripts/selftest.js"
  },
  "engines": { "node": ">=20" }
}
