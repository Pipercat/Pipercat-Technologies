{
  "name": "systemone-pi-mvp",
  "version": "0.2.0",
  "private": true,
  "description": "Local-first SystemONE Pi MVP vertical slice with local Philips Hue integration",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check lib/hue.js && node --check lib/storage.js && node --check web/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
