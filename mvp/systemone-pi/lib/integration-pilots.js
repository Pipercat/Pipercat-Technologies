const ORDER=Object.freeze(['hue','govee','matter','shelly','zigbee']);
const PILOTS=Object.freeze({matter:{phase:'planned',supported:false,adapterBoundary:'MatterAdapter',hardwareMatrix:[{controller:'Raspberry Pi Thread/Matter controller',model:null,firmware:null,status:'not-tested'}],security:['Commissioning-Codes nur lokal und redigiert','Fabric-Schlüssel verlassen das Gerät nicht','IPv6-/mDNS-Ziele strikt lokal'],abort:['Kein reproduzierbares Commissioning','Controller benötigt Herstellercloud','Fabric-Recovery unklar']},shelly:{phase:'planned',supported:false,adapterBoundary:'ShellyAdapter',hardwareMatrix:[{controller:'LAN',model:null,firmware:null,status:'not-tested'}],security:['Nur private LAN-IP','Gen1/Gen2 RPC getrennt validieren','Authdaten nur lokal geschützt'],abort:['Modell/Firmware nicht eindeutig','Cloudpflicht','Unauthentifizierter Schreibzugriff nicht sicher begrenzbar']},zigbee:{phase:'planned',supported:false,adapterBoundary:'ZigbeeAdapter',hardwareMatrix:[{controller:null,model:null,firmware:null,status:'not-tested'}],security:['Koordinator exklusiv gebunden','Netzwerkschlüssel lokal geschützt','Join nur zeitlich begrenzt und sichtbar'],abort:['Koordinator/Firmware nicht reproduzierbar','Unsicheres dauerhaftes Permit-Join','Herstellerdaten dringen in Core ein']}});
function publicPilotPlan(){return{order:[...ORDER],current:'govee',next:'matter',pilots:Object.fromEntries(Object.entries(PILOTS).map(([id,item])=>[id,structuredClone(item)])),releaseRule:'supported=true erst nach vollständiger Hardwarematrix und dokumentierten Fehlerfällen'}}
function validatePilotPlan(plan=publicPilotPlan()){if(plan.order.join('>')!=='hue>govee>matter>shelly>zigbee')throw new Error('Integrationsreihenfolge ist ungültig.');for(const id of ['matter','shelly','zigbee']){const item=plan.pilots[id];if(item.supported||!item.adapterBoundary.endsWith('Adapter')||!item.hardwareMatrix.length||item.hardwareMatrix.some(row=>row.status!=='not-tested')||!item.security.length||!item.abort.length)throw new Error(`${id}-Pilot ist vor Hardwaretest unzulässig freigegeben.`)}return true}
module.exports={ORDER,PILOTS,publicPilotPlan,validatePilotPlan};
